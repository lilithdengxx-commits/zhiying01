# 🦅 智鹰 · ToG政企大客户AI商机挖掘系统
## 运行指南（压缩包版）

---

## 📋 前置要求

只需安装一个软件：**Node.js 20 LTS**

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10 / 11（64位） |
| Node.js | >= 20.x LTS |
| 内存 | 建议 4GB 以上 |
| 网络 | 需要联网（用于安装依赖和实时抓取数据） |
| 端口 | 3001（后端）、5173（前端）需未被占用 |

---

## 🚀 第一次运行（三步完成）

### 第一步：安装 Node.js

1. 打开浏览器，访问 https://nodejs.org/
2. 点击 **"20.x.x LTS"** 下载 Windows 安装包（.msi）
3. 双击安装包，全部点击"Next"默认安装即可
4. 安装完成后，打开"命令提示符"（Win+R 输入 cmd），输入以下命令验证：

```
node -v
```

看到类似 `v20.x.x` 的版本号即为安装成功。

---

### 第二步：安装项目依赖

解压压缩包到任意目录（**建议英文路径**，如 `D:\zhiying`），然后：

- 双击 **`一键安装依赖.bat`**
- 等待窗口自动完成前后端依赖安装（约 2–5 分钟，取决于网速）
- 看到"全部依赖安装完成"提示后关闭窗口

> ⚠️ 此步骤只需要执行一次，后续启动无需重复安装。

---

### 第三步：启动系统

- 双击 **`一键启动全部.bat`**
- 脚本会自动打开两个窗口分别运行后端和前端
- 约 5 秒后浏览器将自动打开 **http://localhost:5173**

看到"智鹰"系统界面即启动成功 🎉

---

## 🖥️ 系统功能说明

| 导航模块 | 地址 | 功能说明 |
|----------|------|----------|
| 🏠 工作台 | `/` | 今日商机概览、待办提醒、商机漏斗 |
| 🧠 智能线索池 | `/leads` | AI评分线索、多维筛选（含信号源筛选）、状态流转 |
| 📡 政策雷达 | `/policy` | 政策解析、商机任务提取、预算监控 |
| 🔍 标讯雷达 | `/bidding` | 招采监控、友商透视、分包预警 |
| 👥 客户管理 | `/graph` | 存量客户档案、二次商机预测 |
| 📅 营销日历 | `/calendar` | 展会抓取、日历视图 |
| 📰 每日简报 | `/briefing` | 每日商机摘要、高优线索推送 |

---

## 🔗 服务地址

| 服务 | 地址 |
|------|------|
| 前端页面 | http://localhost:5173 |
| 后端 API | http://localhost:3001 |
| 健康检查 | http://localhost:3001/api/health |
| 线索接口 | http://localhost:3001/api/leads |
| 爬虫状态 | http://localhost:3001/api/crawler/status |

---

## 🛑 停止系统

关闭这两个命令行窗口即可停止运行：
- 标题为 **"智鹰-后端API"** 的窗口
- 标题为 **"智鹰-前端"** 的窗口

---

## ❓ 常见问题

### Q：双击 bat 文件一闪而过，什么都没发生？
**A：** 右键点击 bat 文件 → 选择"以管理员身份运行"。

---

### Q：浏览器打开了但页面空白或报错？
**A：** 检查后端是否已启动成功：
1. 打开浏览器访问 http://localhost:3001/api/health
2. 若返回 JSON 数据则后端正常，刷新前端页面即可
3. 若无法访问，请先确保"智鹰-后端API"窗口已打开且无红色报错

---

### Q：提示"端口已被占用"？
**A：** 3001 或 5173 端口被其他程序占用，关闭相关程序后重新启动；或重启电脑后再试。

---

### Q：依赖安装失败，提示网络错误？
**A：** npm 默认使用国外镜像，网络不稳定时可能失败。解决方法：
1. 打开命令提示符，运行以下命令切换为国内镜像：
```
npm config set registry https://registry.npmmirror.com
```
2. 然后重新双击"一键安装依赖.bat"

---

### Q：线索池/政策雷达数据为空？
**A：** 系统连接了政府采购网（ccgp.gov.cn）和新华网实时数据源，首次启动后约 30 秒完成第一次抓取。若长时间为空：
1. 检查网络是否可以访问 www.ccgp.gov.cn
2. 访问 http://localhost:3001/api/crawler/status 查看抓取状态
3. 访问 http://localhost:3001/api/crawler/run 手动触发一次抓取

---

## 📁 项目文件说明

```
解压目录/
├── 一键安装依赖.bat     ← 首次运行，安装依赖
├── 一键启动全部.bat     ← 日常使用，启动全部服务
├── 启动前端.bat         ← 单独启动前端（备用）
├── 启动后端.bat         ← 单独启动后端（备用）
├── 运行指南.md          ← 本文件
├── README.md            ← 项目说明
├── backend/             ← 后端服务（Express + 爬虫）
│   ├── server.js
│   └── scrapers/
│       ├── ccgp.js      ← 政府采购网抓取器
│       └── xinhua.js    ← 新华网抓取器
└── frontend/            ← 前端应用（React + Ant Design）
    └── src/
        ├── api.js        ← API层（USE_MOCK=false 真实数据模式）
        ├── mockData.js   ← 本地模拟数据（备用）
        └── pages/        ← 各功能页面
```

---

## 📞 技术参数

| 参数 | 说明 |
|------|------|
| 前端框架 | React 18 + Ant Design 5 + Vite 5 |
| 后端框架 | Node.js + Express 4 |
| 数据模式 | 实时模式（`USE_MOCK=false`） |
| 数据来源 | 政府采购网 + 新华网信息公开 + 内置模拟数据 |
| 定时抓取 | 每 30 分钟自动刷新一次 |

---

*智鹰系统 · 产品原型 v1.0 · 2026年3月*
